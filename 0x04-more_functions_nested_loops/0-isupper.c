#include "main.h"
/**
 * _isupper - checks for upper case letters
 * @c: characters
 * Return: 1 for uppercase, 0 else
 */

int _isupper(int c)
{
	char i;

	for (i = 'A'; i <= 'Z'; i++)
	{
		if (c == i)
		{
			Return 1;
		}
		else
		{
			Return 0;
		}
	}
}
