#include "main.h"
/**
 * _isdigit - checks for digit
 * @c: is an integer
 * Return: 1 for integer, 0 else
 */

int _isdigit(int c)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		if (c == i)
		{
			Return 1;
		}
		else
		{
			Return 0;
		}
	}
}
